# Silicon Forecast handover — 2026-08-24

## Completion addendum — 2026-08-25

This handover's `mx-4` work is complete, merged, deployed and independently verified. The historical sections below remain useful audit context but their “pending”, “unmerged” and “not deployed” statements are superseded by this addendum and `.planning/HANDOFF.json` schema version 5.

### Canonical release

- `origin/main`: `e1dbe6ddc9167046d1bb5b8f1ff9a249f0e4fb86`
- reviewed release ancestor: `eb56fdb638a3aa8fede2e8e169b7ca2b666266e3`
- feature head retained at `7af0bdaf15a1cf0837eb9eaad644b17d52baf438`
- PRs #1–#4 merged normally; no force-push, rebase or history rewriting
- exact release promotion: 44 → 65 factual observations across three exact MPNs
- promotion: exactly 21 reviewed observations; promotion queue now zero
- private exceptions: 40; all `£99,999.99` sentinels remain quarantined
- retailer comparison: three rows, eight approved retailer columns and eight populated exact-pair cells

### Collector result and rotation

The natural 24 August launchd run completed successfully as `20260824T103006Z` in commit `0eaa292954f732660f64eaaf732f7ee0e645d1d6`. Its 35 observations and 35 evidence entries are append-only and bound by tranche SHA-256 `6ca15084f8eeb363bfb47040a227e0fd0aec84a03507fd85f3ed1b9a4b34e4a6`.

The prior collector selected the same first 45 of 86 eligible targets, starving 41. Fixture-first regressions now prove deterministic retailer-plus-normalised-MPN rotation, wraparound, legacy migration, registry change handling, duplicate fail-closed behaviour and cursor advancement only after committed success.

Launchd label `uk.co.siliconforecast.collector` remains the sole prospective fetcher. At `2026-08-25T06:32:29Z` it was loaded, idle, lock-free, had eight recorded runs and last exit code 0. The dedicated checkout remained clean at `0eaa292`; it was deliberately not fetched or modified before the pending 25 August 11:30 local run.

### Exact deployment

- approval: `sf-factual-offers-retailer-comparison-deployment-2026-08-25-v2`
- approval SHA-256: `fdc782fa89b75e61043453bd41807c37d973589aee5c4805f63f9e3c21713b37`
- source SHA-256: `f2f8b9e676b1a446dce874cec61777273f4993f5d252b31492373f552ca61618` across 96 files
- upload SHA-256: `ec5e432618862be134db35d97c2ddd8c314ca800bc0fec3775c9f9b5ac36a794` across 116 regular files
- Cloudflare version: `9b839548-d4e1-42f0-b6b0-6083e71074a7`
- production: `https://siliconforecast.com/`

A clean canonical-main clone reproduced both digests, passed the approval check and Wrangler dry-run, then deployed the exact upload. The homepage, RAM workspace, exact-MPN page and research page returned HTTP 200 and matched approved local HTML bytes. `www` and plaintext HTTP return 308 to the HTTPS apex. The live retailer matrix wording is present; recommendation, live-stock and formal-index-live claims are absent.

### Locks retained

No authority was granted for aggregate index publication, methodology, thresholds, basket, baseline, historical reference, deflator, source-family portfolio, recommendation, event markers, editorial causal claims, current-price/current-stock claims, production database mutation, or a new region/category.

### Next action

Observe the natural 25 August launchd run after it completes and audit its immutable tranche before accepting any derived effect. Do not manually fetch, create a competing scheduler or touch the dedicated checkout before/during the run. Separately triage the one high-severity `npm ci` advisory without applying an unreviewed dependency mutation.

## Resume instruction

Continue Silicon Forecast from:

1. `AGENTS.md`
2. `.planning/PROJECT.md`
3. `.planning/REQUIREMENTS.md`
4. `.planning/ROADMAP.md`
5. `.planning/STATE.md`
6. `.planning/HANDOFF.json`
7. `.planning/HANDOVER-2026-08-24.md`
8. `docs/AUTONOMY.md`
9. `docs/BRIEF-SOURCES.md`

Use Jarvis as parent orchestrator. Bounded workers may research, inspect or implement disjoint tasks, but their summaries are untrusted until the parent independently inspects the files, reproduces consequential claims and reruns verification.

## One-sentence execution boundary

Continue expanding truthful exact-MPN RAM evidence and repair deterministic collector target rotation, while launchd remains the sole prospective fetcher and all methodology, index, source-family, basket, baseline, reference, deflator, recommendation, editorial and production actions remain independently locked.

## Immediate timing warning

Handover prepared at `2026-08-24T10:24:28+01:00` (`09:24:28Z`).

The canonical launchd collector is scheduled for **11:30 BST today**. It is loaded and idle at handover, with seven completed runs and last exit code 0.

Before or during that pending run:

- do not launch any prospective fetch;
- do not run the collector manually;
- do not create another scheduler;
- do not modify, rebase or switch the dedicated collector checkout;
- do not create a competing tranche for any retailer/product/date.

Read-only code analysis is safe. After launchd completes, audit its exact output and Git commit before changing collection logic.

## Product North Star

Prove one lawful, accurate and reproducible UK regional DDR5 price history/index foundation before expanding category or region scope.

The useful public product should feel stock-market-like:

- dated price history;
- obvious lower, typical and higher factual observations;
- exact-product and retailer provenance;
- meaningful pre-surge history when evidence supports it;
- separately reviewed contextual events;
- explicit gaps instead of invented continuity.

“Stock-market-like” describes presentation and exploration. It does not authorise forecasts, recommendations, causal claims, synthetic prices, interpolation or presenting the current elevated market as normative index 100.

## Repository and branch topology

Primary development worktree:

- Path: `/Users/weetoddy/Desktop/Projects/Silicon_Forecast_Recovery`
- Branch: `feat/retailer-comparison-matrix`
- HEAD: `66c1a541f7da6ef4341f78c7b0c70f8b2b3254b2`
- Remote feature: `origin/feat/retailer-comparison-matrix` at the same revision
- Worktree at handover preparation: clean

Feature commits:

- `2051912` — `feat: add exact-MPN retailer comparison`
- `66c1a54` — `docs: record retailer comparison release state`

Current remote main after a fresh fetch:

- `origin/main`: `46ab5c2d2186d0a85ed3516887cceb3e4faddc6f`
- Feature relationship: six main-only collector commits and two feature-only commits (`git rev-list --left-right --count origin/main...HEAD` returned `6 2`).
- Do not assume the feature branch contains the 18–23 August collector runs.

Main-only collector commits since the deployed dashboard revision:

- `5698ca2` — 18 August run
- `4586d01` — 19 August run
- `a501b1a` — 20 August run
- `e80f142` — 21 August run
- `00cc6b7` — 22 August run
- `46ab5c2` — 23 August run

Safe integration approach after the 24 August natural run completes:

1. fetch `origin/main`;
2. confirm the collector checkout is idle, clean `main` and equal to `origin/main`;
3. inspect the new run and integration audit;
4. merge latest `origin/main` into `feat/retailer-comparison-matrix` without rewriting remote history;
5. resolve only additive manifest/ledger conflicts with exact-byte verification;
6. regenerate only governed derived/public artefacts that have explicit authority;
7. run the full test/build/replay suite;
8. push the verified merge normally—no force push.

Do not casually rebase the already-pushed feature branch. A normal merge is less elegant but preserves the audit trail, which is more useful than an immaculate graph nobody can explain.

## Production state

Canonical URL:

- `https://siliconforecast.com/`

Currently deployed revision:

- `1d222308df0742362ecc301ba263678605822334`

The deployed revision includes:

- factual exact-MPN offer release;
- daily observed-market dashboard;
- monthly averaging for ranges longer than 30 days;
- exact low/high provenance;
- empty reviewed Event Line;
- homepage-first dashboard and RAM drill-down;
- checksum-bound deployment approval.

Cloudflare deployment/version last verified for that release:

- `ca62f360-ff41-460d-82b7-902e0859dbb7`

The retailer-comparison feature is **not merged or deployed**. Its changed deployment surface deliberately fails against the prior approval. Do not modify deployment approval or deploy without a new explicit human decision bound to the exact final digest.

## Approved retailer-comparison candidate

David approved this public comparison axis on 24 August 2026:

1. AWD-IT
2. Box.co.uk
3. CCL
4. Currys
5. KingstonMemoryShop
6. Novatech
7. Overclockers UK
8. Scan

This approval authorises:

- displaying these retailer names as matrix columns;
- exact released MPNs as rows;
- the latest already-released qualifying observation in each exact MPN/retailer cell;
- an honest blank when no qualifying released observation exists;
- exactly three deterministic lower/middle/higher factual spotlights when at least three released products exist.

It does **not** authorise:

- fabricating or forward-filling missing cells;
- claiming a retailer does not stock an MPN merely because a cell is blank;
- claiming complete retailer or market coverage;
- marketplace or second-hand inventory;
- buying recommendations, “best deal”, ranking or value claims;
- prospective fetching outside the canonical launchd collector;
- source-family approval, methodology approval or deployment.

Key comparison files:

- `config/retailer-comparison-roster.v1.json`
- `schemas/retailer-comparison.v1.schema.json`
- `scripts/build-retailer-comparison.mjs`
- `data/public-offers/retailer-comparison-ram.v1.json`
- `data/public-offers/retailer-comparison-ram.manifest.v1.json`
- `lib/public-offer-view.ts`
- `components/offers/OfferMatrix.tsx`
- `components/offers/OfferCard.tsx`
- `app/price-history/[slug]/page.tsx`
- `tests/public-offer-view.test.mjs`

Current feature-branch public comparison state:

- 3 released exact MPN rows;
- 8 approved retailer columns;
- 8 populated exact MPN/retailer pairs;
- other cells remain explicit blanks;
- exactly 3 unique factual spotlights.

The selector first reduces observations to one latest qualifying record per exact pair/product, then applies deterministic code-point tie-breaking. Locale-dependent sorting was removed during adversarial review.

## Current public artefacts on the feature branch

The feature branch intentionally retains the currently approved factual release rather than automatically publishing later collector output:

- `data/public-offers/offers-ram.v1.json`: 44 observations, 3 products, 4 represented retailers
- `data/public-offers/retailer-comparison-ram.v1.json`: 8 approved display axes
- `data/public-dashboard/daily-market-ram.v1.json`: 29 daily points in the committed projection
- `data/public-dashboard/event-line-ram.v1.json`: 0 reviewed markers
- private/public-offer exceptions at the prior release: 33

Later collector tranches are candidate evidence. They do not become public merely by arriving on `main`.

## Collector state

Canonical scheduler:

- system: launchd
- label: `uk.co.siliconforecast.collector`
- schedule: daily at 11:30 local time
- `RunAtLoad`: false
- state at handover: loaded, not running
- recorded runs: 7
- last exit code: 0
- Hermes collector job: none

Dedicated checkout:

- `/Users/weetoddy/Desktop/Projects/Silicon_Forecast_Collector`
- branch: clean `main`
- HEAD at handover: `46ab5c2d2186d0a85ed3516887cceb3e4faddc6f`
- collector checkout `origin/main`: same revision

Latest completed run before handover:

- run: `20260823T103006Z`
- commit: `46ab5c2`
- retained observations: 35
- represented retailers: 3
- abstentions: 10
- result: committed and pushed, all approval locks unchanged

Latest observation tranche:

- `data/observations/candidate/uk-primary-retail-20260823T103006Z.v1.json`

Latest integration audit:

- `research/audits/uk-primary-retail-global-integration-audit-20260823T103006Z.v1.json`

Collector safety invariants:

- clean dedicated `main` checkout required;
- fast-forward-only sync with exact `origin/main`;
- local-ahead and divergence fail closed;
- atomic overlap lock;
- preflight before retailer requests;
- additive immutable tranches and ledgers;
- push failure retains evidence locally and exits non-zero;
- no second scheduler or ad-hoc prospective fetch.

## Remaining active task: mx-4

Expand exact-MPN publication from existing retained evidence and fix collector roster rotation.

### Problem A — collector breadth

Natural runs appear to select the first 45 eligible targets repeatedly rather than rotating fairly through the wider target registry. The 23 August run attempted 45 targets, retained 35 observations from 3 retailers and abstained on 10.

The next context must prove the selection behaviour from code and run ledgers before changing it. Do not infer the bug solely from similar counts.

Desired properties for a candidate fix:

- deterministic ordering and replay;
- durable cursor or coverage state with exact lineage;
- no overlap with an active run;
- no starvation of later eligible targets;
- stable behaviour when targets are added, removed, held or abstain;
- bounded daily request volume;
- failed runs must not silently advance coverage;
- no duplicate same-day retailer/MPN requests unless explicitly represented by the governed target contract;
- fixture-first tests with no live credentials or retailer requests;
- collector checkout remains untouched until the pending natural run completes.

Likely files to inspect after merging current main:

- `scripts/run-canonical-collector.mjs`
- collector target registry/config files discovered by that script
- `data/collection-runs/ledger.v1.json`
- `data/derived/private-candidate/ram-input-manifest.v1.json`
- collector and global-integration tests

Do not select a rotation threshold or persistence rule merely because it is easy. Implement candidate policies as explicit deterministic contracts and retain owner decision points where material.

### Problem B — broader exact-MPN publication

Existing holdings are materially broader than the public release. Prior audit found approximately:

- 1,604 candidate observations;
- 251 MPN strings;
- 604 Scan observations;
- 728 multi-retailer observations;
- 47 archive observations;
- 99 movements;
- three £99,999.99 sentinel values requiring quarantine.

These figures predate the 18–23 August collector commits and must be recomputed after branch integration. Do not repeat them as current totals without a fresh deterministic audit.

Promotion rules remain strict:

- exact MPN;
- reviewed 32GB (2×16GB) DDR5 desktop-kit identity;
- UK primary retail only;
- retailer-owned stock, not marketplace/second-hand;
- explicit VAT state;
- dated source with response/evidence hash;
- qualifying availability semantics;
- no sentinel price;
- no unresolved collision or contradictory evidence;
- checksum-bound policy/product approval;
- publication generator must abstain rather than infer.

Recommended bounded sequence:

1. Recompute holdings by exact MPN, retailer, date and evidence quality after merging collector commits.
2. Produce a private eligibility matrix explaining why each candidate MPN passes or abstains.
3. Prioritise already-reviewed MPNs with multi-retailer evidence; do not choose a basket.
4. Prepare additive identity/review packets for promising exact MPNs that lack product approval.
5. Present promotion candidates and abstentions for explicit human approval.
6. Only after approval, regenerate factual offers, daily dashboard and comparison artefacts.
7. Verify that spotlight selection and matrix rows expand deterministically.
8. Treat every changed public/deployment digest as requiring separate approval.

## Locks that remain engaged

No current approval selects or authorises:

- supported formal index publication;
- methodology thresholds;
- temporal outlier thresholds beyond the known sentinel quarantine;
- basket composition or vintage;
- baseline or historical reference;
- linking operator;
- monthly completeness rule;
- deflator or release-vintage policy;
- earnings-relative measure;
- source-family/portfolio approval;
- complete-market claims;
- recommendations or rankings;
- causal event claims;
- live Event Line markers;
- production database mutation;
- spend, affiliate tracking or automatic programme action;
- a new region/category;
- production deployment of the comparison feature.

The approved Daily Market Dashboard calculation remains:

- retailer/product/day reduction;
- product medians;
- equal-product category median;
- low/high from retained qualifying observations;
- direction only when adjacent days contain the identical exact-MPN set;
- daily points for 7D/30D;
- one deterministic observed-month average for longer ranges;
- explicit gaps with no interpolation or line bridging.

Do not reinterpret this as a formal index methodology.

## Known historical facts that must not regress

- 11 August 2026 remains `unobserved_no_run`.
- £99,999.99 placeholder observations remain quarantined.
- Missing dates/months are never interpolated, connected, forward-filled or backcast.
- Archive capture time is not treated as the exact retailer price-change time.
- Public low/high and matrix cells retain exact retailer/source evidence.
- Event Line remains empty until separately cited, reviewed and approved.
- Public pages do not expose internal worker details, private exception reasons, matching state or candidate-only prices.
- Marketplace and second-hand channels remain private/deferred.
- Public MPN disclosure is allowed; matching-state disclosure is not.
- JSON Schemas use draft-07 and must remain compatible with Ajv 6.x.

## Verification baseline for the comparison candidate

Immediately before commit/push:

- production static build: passed;
- TypeScript: passed;
- deterministic factual-offer replay: passed;
- Daily Market replay: passed;
- Event Line replay: passed with 0 markers;
- retailer-comparison replay: passed with 8 approved axes;
- public-boundary checks: passed;
- rendered-content checks: passed;
- final focused/static suite: 75/75 passed;
- desktop visual review: passed; all 8 retailer columns fit at 1280px;
- mobile visual review: passed at 390×844; cards stack and matrix scroll stays contained;
- adversarial review: completed; exact authority keys, exact roster tuples, hard prohibitions and locale-independent sorting were hardened;
- `git diff --check`: passed;
- prior deployment approval: correctly fails closed against the changed surface.

Only warning:

- Node emits `MODULE_TYPELESS_PACKAGE_JSON` performance warnings for imported TypeScript modules; no functional failure.

## First actions in the next context

### Wave 0 — before 11:30 run or while it is active

Parent only:

```bash
cd /Users/weetoddy/Desktop/Projects/Silicon_Forecast_Recovery
git status --short --branch
git fetch origin main
git rev-list --left-right --count origin/main...HEAD
launchctl print gui/$(id -u)/uk.co.siliconforecast.collector
```

Then inspect the collector checkout read-only:

```bash
git -C /Users/weetoddy/Desktop/Projects/Silicon_Forecast_Collector status --short --branch
git -C /Users/weetoddy/Desktop/Projects/Silicon_Forecast_Collector rev-parse HEAD
git -C /Users/weetoddy/Desktop/Projects/Silicon_Forecast_Collector rev-parse origin/main
```

If the collector is pending/running, do only read-only code/ledger analysis. Do not modify its checkout or make retailer requests.

### Wave 1 — after the 24 August run completes

1. Record launchd state, exit code and run count.
2. Identify the exact new Git commit, tranche, evidence ledger and integration audit.
3. Independently validate attempted/retained/abstained counts and immutable hashes.
4. Check whether the same first 45 targets were selected and prove why from code/state.
5. Fetch latest `origin/main` in the feature worktree.
6. Merge latest `origin/main` into the feature branch normally.
7. Run focused manifest, collector and public-boundary tests.

### Wave 2 — bounded parallel work

Use no more than three disjoint workers:

1. rotation/state-machine audit with fixture-only candidate tests;
2. holdings/eligibility audit by exact MPN and retailer;
3. adversarial governance review of proposed rotation and publication expansion.

Workers may not fetch, approve products/sources/methodology or edit shared planning files. Parent integrates and verifies one result at a time.

## Suggested prompt for the new context

> Continue Silicon Forecast from `.planning/HANDOFF.json`, `.planning/STATE.md` and `.planning/HANDOVER-2026-08-24.md`. Jarvis remains parent orchestrator and worker outputs are untrusted until independently integrated and verified. Start with Wave 0 and the live scheduler/Git state. The launchd collector at 11:30 local is the sole prospective fetcher; do not modify its dedicated checkout or launch any fetch before/during its pending run. After it completes, audit the new immutable tranche, fetch current `origin/main`, merge the collector commits into `feat/retailer-comparison-matrix` without rewriting history, and verify the baseline. Then execute `mx-4`: prove and fix deterministic target rotation using fixture-only tests, recompute exact-MPN publication eligibility from all retained evidence, and present promotion candidates without selecting methodology, baskets, thresholds, references, deflators, source families, public claims or production actions. The retailer comparison candidate is committed and pushed but not merged or deployed; any deployment requires a new exact digest-bound approval.

## Completion criteria for the next context

Before its handover:

- all accepted collector output is independently audited;
- feature branch is reconciled with current main or the blocker is explicit;
- target-rotation behaviour is proven by tests, not assumed;
- any fix is deterministic, bounded, replayable and does not alter an active/pending run;
- holdings/eligibility totals are freshly computed;
- candidate MPN promotions and abstentions are separated and evidence-backed;
- no unapproved public artefact or production action occurs;
- full relevant tests, replay checks, build and `git diff --check` pass;
- worktree, branch topology, scheduler and production state are recorded truthfully.

Prepared: `2026-08-24T09:24:28Z`
