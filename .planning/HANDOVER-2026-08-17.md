# Silicon Forecast governed recovery handover — 2026-08-17

## Status

The recovery branch is independently audited and fully verified, with all numerical publication and production deployment paths physically disabled. This document supersedes the operational instructions in the 2026-08-09 and 2026-08-10 handovers; those remain historical records only.

Recovery worktree: `/Users/weetoddy/Desktop/Projects/Silicon_Forecast_Recovery`

Recovery branch: `recovery/governed-graphs-20260817`

Preserved original branch: `feat/graphs-first-frontend`

## What was recovered

- Four immutable prospective collector commits from 13–16 August were retained without rewriting their evidence.
- The 11 August no-run event remains an explicit gap and must never be reconstructed.
- Salvageable graphs-first frontend work was integrated in an isolated main-based recovery worktree.
- Calendar gaps now split chart paths; missing quarters and stopped chains remain explicit.
- Unapproved dispersion diagnostics were removed from the public boundary.
- Private candidate inputs are exhaustively classified and SHA-256-bound.
- The latest global audit is deterministically rebuilt from every discovered prospective tranche; historical audits remain immutable snapshots.

## Publication and deployment boundary

Publication activation is not implemented in this recovery.

- `lib/publication-gate.ts` always returns withheld.
- `lib/public-data.ts` returns no private candidate index, products or events.
- Category and homepage coverage/status copy obey the same boundary.
- The generated projection remains `private_candidate` and `publication_eligible: false`.
- The projection manifest binds each generated dataset by SHA-256 and byte length.
- `npm run deploy` exits non-zero.
- `.github/workflows/deploy-pages.yml` has no deployment action or pages write permission.
- The former sign-off document is explicitly superseded and cannot activate anything.

A future public release requires a new, separately governed protected approval and deployment design. Editing repository JSON or an environment variable is insufficient by construction.

## Collector ownership

launchd is the sole prospective scheduler. The former Hermes job is removed and must not be restored.

Label: `uk.co.siliconforecast.collector`

Schedule: 13:45 local daily

Current recovery state: unloaded

The hardened collector now requires:

- one dedicated clean checkout on `main`;
- exact cached `origin/main` alignment at installation;
- runtime fetch followed only by fast-forward integration;
- refusal of dirty, wrong-branch, diverged or locally-ahead states;
- an atomic exclusive lock before checkout mutation or retailer requests;
- conservative stale-lock recovery only for a same-host, provably dead PID;
- preflight before all retailer requests;
- a retained local evidence commit if push fails.

The installer renders the plist from the target checkout revision, validates it before bootout, and restores the prior plist, job and checkout metadata if replacement fails. `RunAtLoad` is false, so installation must not trigger a retailer fetch.

## Independent audit outcome

Two bounded rounds of three read-only workers reviewed:

1. publication governance and public/private boundaries;
2. collector, Git and launchd operations;
3. evidence manifests, global audits and graph-gap semantics.

Worker outputs were treated as untrusted. Parent review reproduced the material findings, repaired them and reran focused tests before the full gate. The principal repaired blockers were forgeable repository approvals, unhashed projection datasets, private aggregate leakage, stale-output deployment, installer version skew, incomplete install rollback, permanent stale locks, weak candidate metadata validation and incomplete audit regeneration checks.

## Verification

Completed at `2026-08-17T02:41:03Z`:

- `npm run check`: passed;
- 344 Node tests discovered before build: 333 passed and 11 build-dependent tests skipped as designed;
- static Next.js build: passed, 20 routes generated;
- post-build boundary/content suite: 33/33 passed;
- disposable PostgreSQL verification: passed;
- projection byte replay: passed;
- 117 governed JSON files parsed;
- shell, Node and plist syntax validation: passed;
- `git diff --check`: passed;
- repository secret-shape scan: passed.

The only diagnostic was Node's `MODULE_TYPELESS_PACKAGE_JSON` performance warning for directly imported TypeScript test modules. It is not a correctness failure and changing package module mode during recovery was not justified.

## Locks that remain closed

Recovery does not select or approve:

- source portfolio;
- methodology or thresholds;
- basket or reference period;
- deflator or earnings series;
- historical public series;
- editorial claims;
- production activation;
- publication.

Historical archive evidence remains sparse private candidate evidence. Editorial anchors remain contextual only. There is no interpolation, forward-fill, backcast, fabricated continuity or implied exact price-change date.

## Integration and installation sequence

1. Commit and push the verified recovery branch.
2. Integrate it into `main` without weakening locks.
3. Create or refresh a dedicated checkout from the resulting `origin/main`.
4. Run `ops/install-collector.sh install <dedicated-checkout>`.
5. Do not run the collector manually.
6. Independently verify one launchd label, one installed plist, one checkout binding, exact `main` alignment, the 13:45 local schedule, no active lock and no Hermes collector job.
7. Audit the next naturally scheduled run additively before trusting its integration.

Machine-readable continuation state is in `.planning/HANDOFF.json`.
