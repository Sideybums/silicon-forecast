{
  "name": "silicon-forecast",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/build-public-site-data.mjs --check && next build && node --test tests/public-boundary.test.mjs tests/site-content.test.mjs",
    "build:data": "node scripts/build-public-site-data.mjs",
    "start": "next start",
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs",
    "test:db": "node scripts/test-database.mjs",
    "check": "npm run lint && npm test && npm run build",
    "check:full": "npm run check && npm run test:db",
    "deploy": "node scripts/refuse-production-deploy.mjs",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "eslint": "^9.39.1",
    "eslint-config-next": "16.3.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
