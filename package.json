{
  "name": "silicon-forecast",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/build-public-offers.mjs --check && node scripts/build-daily-market.mjs --check && node scripts/build-event-line.mjs --check && node scripts/build-public-site-data.mjs --check && next build && node --test tests/daily-market.test.mjs tests/event-line.test.mjs tests/public-boundary.test.mjs tests/public-offers.test.mjs tests/site-content.test.mjs",
    "build:data": "node scripts/build-public-offers.mjs && node scripts/build-daily-market.mjs && node scripts/build-event-line.mjs && node scripts/build-public-site-data.mjs",
    "start": "next start",
    "lint": "eslint .",
    "test": "node --test tests/*.test.mjs",
    "test:db": "node scripts/test-database.mjs",
    "check": "npm run lint && npm test && npm run build",
    "check:full": "npm run check && npm run test:db",
    "deploy": "node scripts/deploy-approved-public-preview.mjs",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "next": "16.3.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "ajv": "^6.15.0",
    "eslint": "^9.39.1",
    "eslint-config-next": "16.3.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.119.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
