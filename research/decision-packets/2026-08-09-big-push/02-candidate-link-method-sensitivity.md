# Candidate link-method sensitivity

State: **NONE ACTIVE — PROPOSED_LOCKED**

The synthetic fixture contains two named candidate descriptors:

- `fixture-candidate-ratio-alpha`
- `fixture-candidate-ratio-beta`

Both remain `PROPOSED_LOCKED`; the fixture declares no default, preferred, selected or approved strategy.

The executable test path is closed over one internal synthetic implementation. An exact external synthetic-test approval envelope must bind:

- strategy contract bytes;
- internal implementation ID and code checksum;
- complete link-input bytes;
- approval-envelope bytes.

Caller-supplied executable functions and extra strategy fields fail closed. Changed overlap bytes, changed contract bytes, changed implementation identity/checksum, missing overlap, stale approval, overlapping native boundaries and duplicate dates all fail closed. Successful synthetic calculations preserve the complete prior prefix, exact rational factors, gaps and source revisions.

This is a safety fixture, not a link-method recommendation. No overlap operator, threshold, splice rule, rebase rule or basket-vintage policy is approved for Silicon Forecast.

A future human decision would require empirical approved-source overlap, sensitivity across plausible methods, revision impact, product/retailer coverage, leave-one-out resilience and a separately attributable approval record. None is requested here.
