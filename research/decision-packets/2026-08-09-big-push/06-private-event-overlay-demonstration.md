# Private event-overlay demonstration

State: **PRIVATE SYNTHETIC FIXTURE — EDITORIAL ACTIVATION LOCKED**

The fixture models quarantined source items, exact retained capture bytes, checksum-bound atomic claim revisions, duplicate lineage, contradiction, event revisions, attributable reviews, immutable numeric revisions and append-only overlay revisions.

Five numeric fixture layers are retained separately:

- native vintage;
- linked nominal;
- historical reference;
- constant price;
- affordability.

The baseline overlay is draft and unavailable. Presentation order is empty. Add cannot create a reviewed overlay; edit and reorder return to `review_pending`; only a different attributable human fixture editor can create `reviewed_private`; unresolved or withdrawn overlays cannot enter presentation.

All add/edit/reorder/review/remove operations preserve every numeric byte and checksum. Parent adversarial review also proved that a self-promoting reviewed add is rejected. Event content cannot provide numeric values, prices, weights, baskets, links, references, deflators, interpolation, gap fill or calculations.

The fixture contains hostile-source instructions as inert quarantined text and does not execute them. Temporal association remains non-causal; contradictory evidence and uncertainty stay visible.

This packet does not approve an event, source, causal claim, editorial workflow, public marker or numeric change. Live event collection and editorial activation remain locked.
