import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { categories } from "@/lib/site";
export function generateStaticParams(){return categories.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const c=categories.find(x=>x.slug===slug);return c?{title:c.name,description:`${c.name} price-history coverage status at Silicon Forecast.`}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=categories.find(x=>x.slug===slug);if(!c)notFound();return <div className="shell page-shell"><header className="category-header"><div><p className="eyebrow">Component category</p><h1>{c.name}</h1><p>{c.summary}</p></div><div className="category-state"><span>{c.status}</span><strong>{c.marker}</strong></div></header><div className="notice"><strong>{c.slug==="ram"?"Methodology research in progress":"Planned coverage"}</strong><span>{c.slug==="ram"?"No live retailer offers or current prices are published yet.":"This category has no tracked or demonstration prices."}</span></div><section className="content-grid"><div><p className="eyebrow">Intended scope</p><h2>Like-for-like before large numbers.</h2></div><div className="prose"><p>{c.detail}</p>{c.slug==="ram"&&<Link href="/price-history">View the synthetic DDR5 chart demonstration →</Link>}</div></section><section className="considerations"><p className="eyebrow">Comparison considerations</p><div>{c.considerations.map((v,i)=><article key={v}><span>0{i+1}</span><p>{v}</p></article>)}</div></section><section className="category-disclosure"><strong>Affiliate and coverage note</strong><p>Future observations may come from authorised affiliate feeds. That would provide a panel of participating merchants, not automatic evidence of the complete UK retail market.</p><Link href="/affiliate-disclosure">Full disclosure →</Link></section></div>}
